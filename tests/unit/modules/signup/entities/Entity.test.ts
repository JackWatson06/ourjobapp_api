
test("we can validate a entity with a phone code", () => {

});

test("we required code to validate an entity which needs a phone code.", () => {

});

test("we can validate a entity with just an email code.", () => {

});
