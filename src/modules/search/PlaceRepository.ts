/**
 * @Delete -- Marked this for deletion. The reason we are not doing it is because it was unecessary. We are not really
 * gaining with using the repository during the searching process of our application.
 *  
 */


// import { read } from "./PlaceMapper";
// /**
//  * Return a list of the domain objects which are similar to the name query.
//  * @param name Name of the place we are searching for.
//  */
// export async function whereSimilar(name: string): Promise<Array<Place>>
// {
//     const similarPlaces: google.maps.places.AutocompleteResponse = await read(name);
//     return placesGroups;
// }